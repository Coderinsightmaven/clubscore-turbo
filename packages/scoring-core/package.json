{
  "name": "@clubscore/scoring-core",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "test": "bun test src/tennis.test.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.2",
    "typescript": "5.9.2"
  }
}
